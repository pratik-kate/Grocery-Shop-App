<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supertrident.ecom">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineSore">
        <activity android:name=".test.MyOrdersActivity"></activity>
        <activity android:name=".test.CheckOutActivity" />
        <activity android:name=".test.ProductDetailActivity" />
        <activity android:name=".test.ShopActivity" />
        <activity android:name=".test.SignUpActivity" />
        <activity android:name=".test.LoginActivity" />
        <activity android:name=".test.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.MainActivity" />
        <activity android:name=".test.BuyActivity" />
    </application>

</manifest>